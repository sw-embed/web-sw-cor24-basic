Create the correct pvm.s: Extract opcode handlers from pvmasm.s (sw-cor24-pcode/vm/pvmasm.s or sw-vibe-coding/pv24a/pvmasm.s) into a bare VM runner that skips the assembler init and just runs pre-loaded p-code. The file should go in asm/pvm.s. It must support all opcodes used by basic.p24: 0x00-0x06, 0x08, 0x10-0x17, 0x20-0x25, 0x30-0x34, 0x40-0x49, 0x50-0x53, 0x60. Use the old pvm.s (sw-cor24-pcode/vm/pvm.s) as a skeleton for the boot/init code (it already knows how to load and run pre-loaded p-code), but replace its dispatch table and handlers with the full set from pvmasm.s. Verify by assembling with cor24-emulator::Assembler in build.rs and checking cargo check passes.