Verify the 'hello' example produces correct output. Run trunk serve, select 'hello', click Run, and check that the output panel shows 'HELLO WORLD' without '>' prompts or other artifacts. The BASIC source is: PRINT "HELLO WORLD" / BYE. The runner feeds this as UART input line-by-line with \r terminators. Strip '>' characters from UART output. Fix any issues with UART feeding, output cleaning, or VM halt detection.