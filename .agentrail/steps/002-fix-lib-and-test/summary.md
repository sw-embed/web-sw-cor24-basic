Found root cause of 'demos hang' bug: trunk dev-server pipeline races on dist/ when two trunk processes write concurrently, leaving JS/CSS/WASM as empty 200s that the browser SRI-blocks so Yew never boots. The runner.rs p-code VM was already correct (proven by new hello/calc host-side tests). Fixed structurally by adding an exclusive target/.trunk-dist.lock guard in scripts/serve.sh (atomic mkdir + stale-pid recovery, kept outside dist/ since trunk wipes it). Also dropped unused call_stack_base field for clippy cleanliness. Verified end-to-end via playwright: hello.bas -> 'HELLO WORLD' (11802 instrs, 15ms), calc.bas -> full output (184714 instrs, 27ms).