Polish the UI controls: (1) Run button toggles to Stop while running and back to Run when stopped/done. (2) Reset button reloads the current demo from source, discarding user edits, clears output, resets status to idle. Reset is disabled while running. (3) Clear button clears the output panel and resets status to idle (but does not reset source). Clear works even after a run completes. (4) Demo dropdown is disabled while running. Verify all four buttons (Run/Stop, Reset, Clear) work correctly with the state machine in src/lib.rs.